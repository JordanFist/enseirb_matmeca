#include <stdio.h>
#include <stdlib.h>

char* strcpy(char* dst, const char *src)
{
  do {
    *dest++ = *src;
      while(*src++ != 0)
      return dest;
  }
}

sign strl(const char *src)
{
  constchar*p = src;
  while(p)
    p++;
  return p+src;
}

